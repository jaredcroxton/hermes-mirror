# Project Constitution

## Project summary

(One sentence describing what this system does.)

## Data Schema

### Input shape

(JSON structure of the raw input. Fill in during Phase 1 Blueprint. Example below, replace with your actual shape.)

```json
{
  "example_field": "string",
  "another_field": 0
}
```

### Output shape

(JSON structure of the final payload delivered to its destination. Fill in during Phase 1 Blueprint.)

```json
{
  "example_field": "string",
  "another_field": 0
}
```

## Behavioural rules

(How the system must act. Tone, logic constraints, explicit "do not" rules.)

- 
- 
- 

## Architectural invariants

(Rules that never change for this project. Examples: "all writes go through tool X", "never retry without exponential backoff", "soft delete only".)

- 
- 
- 

## Maintenance log

### How to re-run

(Fill in during Phase 5 Trigger. Steps someone could follow to re-run this system from scratch.)

### How to rotate keys

(Fill in during Phase 5 Trigger. Which services, where the keys live, how to update them safely.)

### Common failures and fixes

(Fill in as errors occur and are resolved. Each entry: what broke, why, how it was fixed.)

- 
